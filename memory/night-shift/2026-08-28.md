# Night Shift — 2026-08-28

## Pré-flight
- Sistema: disk 59% (40G livres), RAM 3.0G available, 0 failed units, uptime 56d. Saudável.
- Repo ng-m3 limpo em `09d78ff` (Sprint 071 a11y batch #1). Angular 22.1.3 · TS 6.0.3 · Storybook 10.5.10 · nx 23.1.1.
- Pre-shift plano: **a11y batch #2** (button-name restante) + **batch #3** (form labels) + quick-win (deps/TS7). Carry-over da triage `a11y-triage-2026-08-27.md`.
- Nota de método: o mapa por-nó (`/tmp/a11y-map.txt`) desapareceu (era /tmp). Em vez de adivinhar quais controles falham a partir do template, **regenerei a evidência axe** (build-storybook → serve :4400 → test-storybook axe postVisit, filtrado aos componentes-alvo). [[feedback_medir_antes_de_opinar]] [[feedback_verificar_antes_de_alarmar]]

## Frente — Sprint 071: a11y batches #2 + #3 ✅

### Método (medir, não adivinhar)
- Rodei axe filtrado aos 10 componentes-alvo → **baseline: 22 falhas / 15 suites**.
- Onde o rule+contagem não bastava (card/property-card "button-name" com botões que *pareciam* nomeados), instrumentei o test-runner temporariamente p/ logar `target`+`html` do nó, e sondei o DOM vivo via Playwright (`chromium-1234`). Instrumentação **revertida** antes do commit. Isto derrubou 2 suposições erradas minhas (ver abaixo).

### Fixes (correção pura WCAG, additiva)
- **message-thread** — `md-icon-button` de fechar (icon-only, só `<md-icon>close</md-icon>`) sem nome → `aria-label="Fechar conversa"`. + **spec nova** que garante o nome acessível (locka contra regressão). [button-name]
- **deposit-return-estimator** — `<select>` de categoria + 2 inputs (descrição/custo) só com placeholder → `aria-label` por controle. [select-name + label]
- **property-inspection** — `<select>` de estado + input de notas por-quarto, só placeholder → `[attr.aria-label]` interpolado com o nome do quarto. [select-name + label]
- **tax-statement-generator** — 4 inputs placeholder-only (bruto/retenção/descrição/valor) + input `type="date"` + 2 `<select>` → `aria-label` por controle. [label + select-name]
- **rent-escalation-calculator** — input de override de coeficiente por-ano (`re-coef-input`, bare num `<td>`) → `[attr.aria-label]="'Coeficiente para ' + row.year"`. **Suposição derrubada:** os 3 inputs do topo (envoltos em `<label><span>texto</span><input></label>`) SÃO labels implícitos válidos (por isso axe marcava só 2/8/3 nós = as linhas de override, não os do topo). [label]
- **add-property** — `.step-track role="list"` sem filhos `listitem` → `role="listitem"` nos `.step-item`. (Era batch #4/ARIA-structure, mas nó único e trivial; limpa o componente.) [aria-required-children]
- **checkbox** (stories Default/Checked/Indeterminate) e **switch** (Default) — demos com `label:''` deixavam o `md-checkbox`/`md-switch` sem nome. Não é bug do wrapper (não há nome p/ inventar); é autoria de story → `ariaLabel` descritivo nas args. [label]
- **property-card** (story Property Grid) — **suposição derrubada:** o componente está correto (story Default passa com `aria-label` certo). O botão nameless vinha do story-wrapper `@Component` (`PropertyGridWrapper`) que sub-renderizava os cards no test-runner (família dos "render errors" da triage — Playwright confirmou: 1 só `.iu-property-card__fav`, todas as bindings ausentes). Fix: converter o grid p/ o padrão `render:{props,template}` provado (igual Default/Featured); removida a classe wrapper e o import `Component`. [button-name]

### Regressão evitada (candura)
- **card** button-name: raiz REAL = `iu-button` só projeta `<ng-content/>` no case `primary`; os outros variants (ghost/…) renderizam só `{{label()}}`, largando texto projetado → botão ghost sem nome. Tentei o "fix DS-root" (add `<ng-content/>` a todos os cases). **Backfirou:** múltiplos `<ng-content>` em ramos `@switch` quebram a projeção — o botão *primary* ficou vazio (Playwright confirmou "Ver detalhes" sumiu). É exatamente a limitação que o docstring do componente documenta e a razão de existir o input `label`. **Revertido** o componente; corrigidas as **stories** p/ usar o input `label` (API suportada). Lição alinhada com [[feedback_eval_verde_pode_medir_o_caminho_errado]]: medir o efeito real do fix, não presumir.

### Verificação (axe re-run pós-rebuild)
- `nx build-storybook core` → serve :4400 → test-storybook axe (wcag2a/2aa, block critical/serious), `--maxWorkers=2`.
- **10 componentes-alvo: 55/55 story-instances PASS, 0 blocking.** (baseline 22 falhas → 0.)
- `nx build core` ✅ (22.7s). `nx test core` ✅ (ver fecho).

## Gates deferidos (com razão registada)
- **color-contrast (154)** — sistémico, toca paleta/tokens → medição por-nó + ratificação visual do Israel (gated como `M3_EXPRESSIVE_THEME`, [[feedback_ui_bonita_sempre]]). NÃO mass-editar tokens.
- **ARIA structure restante** (aria-required-children restantes, nested-interactive 10, aria-hidden-focus 6, list 4, etc.) — batch #4, dimensionado na triage.
- **render errors** (availability-resource) — ortogonal a a11y.
- **quick-win deps/TS7** — não executado; a frente a11y consumiu a janela com valor real. Deps patch bump e TS7 GA re-probe seguem no carry-over.

## Fecho
- Commit: (hash abaixo).
- Suite ng-m3: 204 suites / 3348 testes verdes (+1 spec nova em message-thread → 3349).
- **Carry-overs:** a11y batch #4 (ARIA structure) · color-contrast (gated Israel) · render-errors · deps patch bump + TS7 GA re-probe · M3_EXPRESSIVE_THEME (ratificação visual).
</content>
</invoke>
