# Night Shift — 2026-07-29

## Contexto
Frente additiva esgotada (6º dia: 0 sem story, 0 sem spec) e frente de deps-seguros seca
(todos os outdated com Wanted==Current). Sem "bump verde" possível. Foco do pre-shift:
**prep de major HELD com evidência real, sem merge à main.**

Sistema: disk 52% (47G livres), RAM ~6.4GB avail, 0 failed units. Tudo verde.
Branch base: `main` @ `f3ff376`, working tree clean.

---

## 🎯 Item 1 — express 5: prep + validação runtime (VEREDITO com evidência)

Trabalho feito em branch throwaway `prep/express5-serve-validation` (NÃO mergeada — major
precisa ratificação do Israel, e os patterns `/{*splat}` quebram express 4).

### O que se fez
1. Bump `express ^4.22.2 → ^5.1.0` (instalou **5.2.1**) + `@types/express 4 → 5` (5.0.6). `pnpm install` limpo (peer warnings pré-existentes de TS6/module-federation, sem relação com express).
2. Fix dos wildcards path-to-regexp 8:
   - `apps/dashboard/server.ts`: `app.get('**', static)` e `app.get('**', ssr)` → `'/{*splat}'` (2 ocorrências)
   - `apps/demo/src/server.ts`: `app.use('/**', ...)` → `app.use('/{*splat}', ...)`

### Evidência A/B (isolada, express 5.2.1 real do workspace)
```
❌ OLD app.get('**')      : PathError: Missing parameter name at index 1: **
❌ OLD app.use('/**')     : PathError: Missing parameter name at index 2: /**
✅ NEW app.get('/{*splat}')  : registered OK
✅ NEW app.use('/{*splat}')  : registered OK
✅ LISTEN+ROUTE: GET /some/deep/route → HTTP 200
```
→ **O fix wildcard resolve exatamente o blocker documentado.** Reproduzido o crash `Missing
parameter name` com os patterns antigos; provado o novo pattern regista + serve.

### Validação end-to-end (Angular SSR real, não só camada express)
Descobertas ao tentar build SSR — **camadas de blocker que estavam escondidas atrás do wildcard**:

1. **`esbuild-ssr` (dashboard) tinha option STALE `ssrEntry`** — rejeitada pelo schema do
   `@angular/build` 21/22 (`'ssrEntry' is not found in schema`). Correto = `outputMode: 'server'`
   + `ssr: { entry: '...' }`. **Isto é ORTOGONAL a express** (falharia igual na main). Corrigido
   no project.json na branch throwaway.

2. **`apps/dashboard/server.ts` está STALE (estilo Angular v17-18):** importa `zone.js/node`
   (NÃO instalado — app é zoneless) e usa `CommonEngine`. Build SSR do dashboard falha em
   `Could not resolve "zone.js/node"`. **Precisa reescrita p/ `AngularNodeAppEngine` zoneless**
   (o `demo/src/server.ts` já usa o estilo moderno). Também ortogonal a express.

3. **`apps/demo` já é moderno** (`ssr:{entry}`, `outputMode:server`, `AngularNodeAppEngine`,
   sem zone.js). Usado como caminho limpo p/ validar express 5 end-to-end.

**Build `demo:build:production` → ✅ Successfully** (com express 5 + `/{*splat}`). Warnings só
budget/leaflet pré-existentes.

**Runtime `node dist/apps/demo/server/server.mjs`:**
```
Node Express server listening on http://localhost:4599   ← ARRANCA sem crash (blocker GONE)
GET /favicon.ico → HTTP 200                              ← static middleware express 5 OK
GET / , /some/deep → HTTP 400 "Header host ... is not allowed"
```
O 400 é o **host-allow-list do `AngularNodeAppEngine`** (feature de segurança SSR do Angular),
invocado igual em qualquer versão de express → **NÃO é regressão de express**. Prova que o
request FLUIU pelo express 5 até ao engine Angular (que então aplicou a própria checagem).

### VEREDITO express 5
- ✅ **Camada express/path-to-regexp: RESOLVIDA e provada.** O fix `/{*splat}` é correto e
  suficiente para o crash documentado. Server express 5 arranca e roteia.
- ⚠️ **SSR end-to-end NÃO fica 100% "ready-to-merge" ainda** — não por express, mas por 3
  blockers ortogonais descobertos: (a) `ssrEntry` stale no dashboard [fix trivial já provado],
  (b) `dashboard/server.ts` com `zone.js/node`+CommonEngine stale [precisa migração p/
  AngularNodeAppEngine], (c) host-allow-list do Angular SSR a validar em serve.
- **Recomendação p/ Israel:** merge de express 5 deve vir junto com a modernização do
  `dashboard/server.ts` (item b) — senão o dashboard SSR fica partido independente de express.
  O demo já está pronto. Estimativa: migração do dashboard server.ts ≈ 30-45min, additiva.
- **Não mergeado** (major + patterns incompatíveis com express 4 na main). Branch throwaway
  descartada; main restaurada a express 4.22.2.

---

## ✅ Item 2 — OnPush reconciliation branch (rebase + gates verdes)

Branch `dry-run/onpush-reconciliation` estava **2 commits atrás da main** (eslint patch +
Storybook 10.5.5 — ambos dep bumps, sem overlap com os .ts dos componentes).

- **Rebase limpo** sobre main atual: `5be5693` → `dc57840` (sem conflitos; 1 commit único =
  conversão dos 18 componentes Eager→OnPush).
- **Gate 1 — `nx build core`: ✅** Successfully.
- **Gate 2 — `nx test core --watch=false`: ✅** 191 suites / 3173 tests, todos verdes (107s).
- **Push:** `--force-with-lease origin dry-run/onpush-reconciliation` (dry-run branch, NÃO main;
  origin sincronizado, ratificação do Israel continua one-click).
- Runtime-behavioral (Default→Eager deprecado vira CheckAlways; os 18 são Eager reais) →
  **não merge autónomo, aguarda ratificação.** Branch fresca e verde, sem bit-rot.

## ✅ Item 3 — MCP 2026-07-28 RC scan (nota técnica, zero código ng-m3)

Spec RC finalizada (final 28/07; Tier 1 SDKs dentro de janela de 10 semanas). Breaking changes
relevantes p/ tooling do Ralph a médio prazo (não afeta ng-m3). Detalhe em
`memory/reference_mcp_2026_07_28_rc.md`. TL;DR:
- **Stateless core:** removidos handshake `initialize`/`initialized` + `Mcp-Session-Id`; version/
  caps agora em `_meta` por request; novo `server/discover`; headers `Mcp-Method`/`Mcp-Name`
  p/ routing; Multi Round-Trip (`InputRequiredResult`+`requestState`) substitui SSE streams.
- **Deprecados (annotation-only, ≥12 meses):** Roots, Sampling, Logging → substituídos por
  tool params/resource URIs, LLM API direta, stderr/OpenTelemetry.
- **Tasks:** virou extensão opt-in, lifecycle stateless (`tasks/get|update|cancel`; `tasks/list`
  removido). **MCP Apps:** UIs HTML server-rendered, security-reviewed pré-execução.
- **Auth hardening:** validar `iss` (RFC 9207); DCR exige `application_type`; creds bound ao issuer.
- **Outros:** erro missing-resource `-32002`→`-32602`; JSON Schema 2020-12 full; `$ref` externos
  não auto-dereference.
- **Impacto Ralph:** nada urgente. Relevante SE/quando Ralph construir MCP server próprio (direção
  LAM/agente que AGE) — stateless + Tasks + auth hardening são o novo baseline.

---

## 📊 Resumo do turno
- **Commits à main: 0** (correto — frente additiva esgotada, deps secos; único trabalho mergeável
  seria express 5, que é major + precisa ratificação + patterns incompatíveis com express 4).
- **Branches:** `dry-run/onpush-reconciliation` rebaseada + verde + pushed.
- **Entregáveis:** veredito express 5 com evidência A/B + runtime real; 3 blockers ortogonais
  destapados (ssrEntry stale, dashboard server.ts zone.js/CommonEngine stale, host-allow-list);
  nota MCP RC.
- **Gates:** build core ✅, test core ✅ (191/3173) na branch onpush.
- **Sistema:** estável o turno todo.
