# 🌙 Night Shift — 2026-07-22

## Contexto de entrada
- Pre-shift honesto: frente additiva (stories/specs/flags/deps) **esgotada**; único candidato de valor de produto real = **Features page wiring** (Candidato 1), a confirmar o gap antes de prometer. Candidato 2 (no-empty-function) só se o 1 não render. 4 decisões de lint continuam bloqueadas aguardando Israel.
- Sistema: disk 59%, RAM 6.4 GB livre, tree clean em `b9805e5`.

## Investigação do gap (Candidato 1 — confirmado REAL)
Diff sistemático da Features page (`apps/dashboard/src/app/pages/features-page.component.ts`, 3620 linhas):
- **89** `<section>` de showcase renderizadas na página.
- TOC lateral ("On this page", `tocSections`) listava só **57** — e **fora da ordem do documento**.
- **32 secções renderizadas mas inalcançáveis** pela navegação lateral: rent-arrears, tenant-dashboard, payment-gateway, payment-receipt, landlord-revenue, ssr, notification-center, property-inspection, lease-renewal, property-transaction-cost-calculator, portfolio-tax-lifecycle, maintenance-module, document-upload, error-pages, booking-checkout, e mais 17 (avatar, timeline, date/color-picker, paginator, tag-input, my-bookings/favourites, etc).
- **Bug adicional:** entrada morta `lease-signing-flow` apontava para âncora inexistente (id real da secção = `e-signature`) → clique não fazia scroll para lado nenhum.

## Entrega
Rebuild completo de `tocSections` em **ordem de documento**, **89 entradas**, cada uma com o **feature-flag guard correto** da respetiva secção (derivado dos `@if (flags.X)` do template). `tocItems()` (computed já existente) passa a mostrar exatamente as secções ativas, na ordem em que aparecem ao fazer scroll.
- Mudança **data-only**, atrás da flag existente `FEATURES_PAGE_TOC` — zero mudança de runtime/lógica, zero componente novo, additivo puro.
- Flags novas: **0** (usa a infra existente).
- Corrige 1 link morto + adiciona 32 secções navegáveis + ordena a lista.

## Gate (verde)
- `nx build core` ✅ (20.7s)
- `nx build dashboard` ✅ (44.8s — TS valida os 89 `keyof typeof FeatureFlags`)
- `nx test core --watch=false` ✅ (cache hit; core intocado)
- dashboard não tem target `test` configurado.

## Commit
- `0437325` — feat(features-page): complete + reorder TOC nav to cover all 89 sections
- Push: ✅ `b9805e5..0437325` origin/main

## Números tangíveis
- TOC nav: **57 → 89** entradas (+32 secções agora alcançáveis).
- Links mortos corrigidos: **1** (lease-signing-flow → e-signature).
- Ordenação: lista agora segue a ordem de scroll da página.

## Carry-overs / pendências (inalteradas — aguardam Israel)
1. prefer-on-push (18 err) — mudança de CD real, precisa aval.
2. renames breaking (~20 err) — no-output-native / directive-selector.
3. controlComponents override (b003a5f) — ratificar.
4. no-empty-function em specs (~27 err) — vale o esforço ou aceitar como warnings?

## Verificação runtime + entrega
- Servi o bundle `dist/apps/dashboard/browser` com fallback SPA + Playwright/Chromium (headless) → `/features`.
- **Runtime confirma 89 `.toc-link` renderizados** (= 89 secções). Prova end-to-end, não só type-check.
- Screenshot do sidebar (`/tmp/toc-sidebar.png`) enviado ao Israel via Telegram (sendPhoto, msg 3576) com o resumo.
- Duração total do turno: ~40min (investigação do gap + rebuild + 2 builds + screenshot + entrega).

