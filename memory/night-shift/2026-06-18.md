# Night Shift — 2026-06-18 (batch 20 — spec coverage)

## Contexto
Frente "activar flags" esgotada (139/139 activas). Frente real = **spec coverage** (89/133 ~67% no início). Additive-only: zero mudança a componentes, só `.spec.ts`.

Baseline confirmada antes de começar: **140 suites / 2449 tests verdes**.

## Componentes cobertos (batch 20)

### 1. filter-bar (650L) — carry-over do batch 19
`filter-bar.component.spec.ts` — text/select/date-range/tags filters.
- Render: 1 field por config, controlo correcto por tipo, options do select (placeholder + configuradas), placeholder de texto.
- Select: emissão de `filtersChange`, `hasActiveFilters`, botão clear-all aparece com filtro activo.
- Date-range: emissão de `FilterDateRange` (start/end), null-out ao limpar parte.
- Tags: add no Enter + chip, sem duplicados, sem vazios/whitespace, `removeTag`, `maxTags` (isTagsFull + input escondido), backspace remove último (só com input vazio).
- `clearField` por tipo (text/date-range/tags) + key desconhecida; `clearAll` reset + emit.
- Edge: date-range só com end = activo; accessors com keys ausentes devolvem defaults seguros.

### 2. inventory-checklist (426L)
`inventory-checklist.component.spec.ts` — descritivo do recheio + delta move-out.
- Header + seed (título, summary, 1 secção por sala distinta, 1 row por item).
- moveOutMode off: sem selects, sem deduction pill, sem barra de progresso.
- moveOutMode on: 1 select por item, % inspeccionado no subtitle, `setMoveOutCondition` via change, ignora selecção vazia (jest.spyOn), delta lines + deduction pill após dano.
- Helpers de label: condition/category/severity → PT labels + badge class + fallback p/ valor cru.

### 3. rent-arrears (449L)
`rent-arrears.component.spec.ts` — dashboard de rendas em atraso (componente; service já tinha spec).
- Header + KPIs (totalOutstanding 9275, 4 tenants, 61d médio, 2 críticos) renderizados no strip.
- Lista: 1 card por record, classes de severidade.
- Severity: critical (legal) / high (62d) / medium (45d) / low (18d).
- Filtros: 1 botão por opção, filtrar por click (legal → 1 card), empty state (resolved → 0), botão activo marcado.
- Acções: sendReminder (++count, status), escalateToLegal, markResolved (zera outstanding).
- Payment plan: openPlanForm (instalment sugerido = ceil/4), submitPlan (activa + fecha), não activa com amount 0.
- Helpers: initials (2 letras maiúsculas), statusLabel + fallback.

## Resultado
- **+59 tests** → baseline 2449 → **2508 passed / 2508 total**, 143 suites verdes.
- Componentes com spec: **89 → 92/133 (~69%)** ✅ meta sugerida atingida.
- Build `nx build core`: ✅ clean (16.7s).
- 1 fix durante execução: `spyOn` → `jest.spyOn` (stack é Jest, não Jasmine).

## Notas
- Quick wins/stretch (quick-links-widget, streak-widget, countdown-widget) não atacados — 3 alvos principais cumpriram a meta; deixar como carry-over leve para batch 21.
